(function($) {
  // Custom form validation instance
  let formSettings = {formErrorSelector: '.c-form__error-text'};
  let form = new Foundation.Abide($('.c-form--validate'), formSettings); // eslint-disable-line no-unused-vars
})(jQuery);
